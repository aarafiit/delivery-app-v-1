// Placeholder — full widget tests are added in later tasks
// once screens and theme are wired up.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
